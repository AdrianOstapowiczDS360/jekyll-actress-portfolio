# Jekyll Actress Portfolio

To run locally:
1. Install dependencies: `gem install bundler && bundle install`
2. Run server: `bundle exec jekyll serve`
3. Open http://localhost:4000/ and http://localhost:4000/en/
